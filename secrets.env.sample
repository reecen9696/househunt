# Copy to secrets.env (gitignored). Loaded into the environment at startup.

# Required for the default scrapedo fetcher (realestate.com.au bot
# protection). Same token the propertypath-backend uses.
SCRAPE_DO_TOKEN=

# Only if you wire in a Browserless-style remote fetcher.
#BROWSERLESS_API_TOKEN=
